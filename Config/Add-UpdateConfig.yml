info:
  name: Add/UpdateConfig
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/api/users/config
  body:
    type: json
    data: |-
      {
        "type": "LOCAL",
        "modelName": "qwen2.5-coder-7b-instruct",
        "baseURL": "http://127.0.0.1:1234/v1",
        "apiKey": ""
      }
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
