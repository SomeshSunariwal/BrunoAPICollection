info:
  name: getUserAllDocuments
  type: http
  seq: 4

http:
  method: GET
  url: localhost:8080/api/users/documents
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
