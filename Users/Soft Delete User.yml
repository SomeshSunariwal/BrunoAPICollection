info:
  name: Soft Delete User
  type: http
  seq: 4

http:
  method: DELETE
  url: localhost:8080/api/users/delete
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
