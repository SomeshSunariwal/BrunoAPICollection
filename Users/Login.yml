info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "some@11.com",
        "passphrase": "1231"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
